language: objective-c

branches:
  only:
    - master

xcode_project: Psiphon-Browser.xcodeproj
osx_image: xcode8

script:
  - xcodebuild clean build -project Psiphon-Browser.xcodeproj CODE_SIGNING_REQUIRED=NO PROVISIONING_PROFILE_REQUIRED=NO
